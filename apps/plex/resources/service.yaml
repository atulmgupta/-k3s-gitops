apiVersion: v1
kind: Service
metadata:
  name: plex
spec:
  type: ClusterIP
  ports:
    - port: 32400
      targetPort: pms
      protocol: TCP
      name: pms
  selector:
    app.kubernetes.io/name: plex
