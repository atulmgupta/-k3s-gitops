apiVersion: v1
kind: Service
metadata:
  name: twofauth
  labels:
    app.kubernetes.io/name: twofauth
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  selector:
    app.kubernetes.io/name: twofauth
